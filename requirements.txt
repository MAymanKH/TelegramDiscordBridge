pyrofork>=2.0.106
discord.py>=2.4.0
TgCrypto>=1.2.5
pyyaml>=6.0.2
aiosqlite>=0.19.0
neonize>=0.3.15
# Link enrichment (optional `enrich:` bridge feature):
#   aiohttp — fetch fxtwitter JSON + download X media (also a discord.py dep)
#   yt-dlp  — download Facebook (and other) media
aiohttp>=3.9.0
yt-dlp>=2024.1.0
# Voice transcription (optional `voice_transcription:` platform feature):
#   provider=openai needs no extra dep (uses aiohttp).
#   provider=local needs faster-whisper — uncomment if you want self-hosted:
# faster-whisper>=1.0.0